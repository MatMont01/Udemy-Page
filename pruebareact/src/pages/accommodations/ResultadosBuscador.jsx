import React from 'react';
import {Container, Row, Col, Card, Carousel, Button} from 'react-bootstrap';

const ResultadosBuscador = ({resultados}) => {
    return (
        <Container className="mt-4">
        </Container>
    );
};

export default ResultadosBuscador;
